import { ShowcontrolsDirective } from './showcontrols.directive';

describe('ShowcontrolsDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowcontrolsDirective();
    expect(directive).toBeTruthy();
  });
});
